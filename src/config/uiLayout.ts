// Раскладка UIScene: все отступы и размеры интерфейса в одном месте.
// Координаты ниже — в пикселях канваса (1:1 к CSS-пикселям).
// К элементам верха прибавляется safeTop (Dynamic Island / чёлка),
// к элементам низа вычитается safeBottom (home-индикатор) — это делает сцена.

/** Верх: кристалл, сумка, строки статов */
export const UI_TOP = {
  /** Высота верхней вуали сверх safeTop */
  veilHeight: 78,
  /** Центр полоски кристалла от safeTop */
  crystalY: 16,
  /** Высота полоски кристалла */
  crystalHeight: 12,
  /** Левый край текстов и полоски */
  leftMargin: 12,
  /** Место справа под колонку сумки */
  bagColumnWidth: 88,
  /** Центр сумки от правого края */
  bagX: 36,
  /** Центр сумки от safeTop */
  bagY: 28,
  /** Бейдж очков под сумкой, от safeTop */
  badgeY: 54,
  /** Строка «золото · волна» от safeTop */
  rowB: 44,
  /** Строка «таймер · убийства · урон» от safeTop */
  rowC: 62,
  /** Зазор между подписями в одной строке */
  rowGap: 10,
  /** Текст обратного отсчёта волны от safeTop */
  intermissionY: 104,
  /** Подсказка про лавку от safeTop */
  shopHintY: 126,
  /** Верх свободной зоны для баннера, от safeTop */
  bannerTopPad: 140,
  /** Низ свободной зоны для баннера, от верха нижнего бара */
  bannerBottomPad: 30,
  /** Позиция баннера: доля от свободной зоны */
  bannerRatio: 0.28,
  /** Запасной центр баннера (тесные экраны): середина минус это */
  bannerFallbackLift: 60,
} as const;

/** Низ: глухой бар — опыт, уровень, колбы */
export const UI_BOTTOM = {
  /** Полоска опыта от нижнего бара */
  xpBarY: 3,
  /** Высота полоски опыта */
  xpBarHeight: 8,
  /** Центр бейджа уровня от низа бара */
  levelBadgeY: 50,
  /** Радиус бейджа уровня */
  levelBadgeRadius: 20,
  /** Подпись XP от верха бара */
  xpTextY: 20,
  /** Центры колб от верха бара */
  orbY: 0,
  /** Радиус колб здоровья/маны */
  orbRadius: 50,
  /** Центр HP-колбы от левого края */
  orbHpX: 70,
  /** Центр мана-колбы от правого края */
  orbManaRightRoom: 200,
  /** Подписи HP/MP от верха бара */
  orbCapY: 34,
} as const;

/** Джойстик слева внизу */
export const UI_JOY = {
  baseRadius: 58,
  knobRadius: 26,
  marginX: 16,
  bottomMargin: 14,
} as const;

/** Кнопка атаки — верхняя в правой группе */
export const UI_ATTACK = {
  radius: 40,
  /** Центр от правого края (место под правый навык) */
  rightRoom: 103,
  /** Центр от низа экрана (под ней веер навыков) */
  lift: 150,
} as const;

/** Навыки веером под атакой: 0 — справа-внизу, 1 — слева-внизу, 2 — под атакой */
export const UI_SKILLS = {
  radius: 34,
  /** Боковые навыки в стороны от атаки */
  dx: 62,
  /** Боковые навыки ниже атаки */
  dySide: 52,
  /** Нижний навык под атакой */
  dyBottom: 96,
} as const;

/**
 * Дополнительный подъём всего нижнего бара над safe-area-inset-bottom.
 * Применяется поверх safeBottom к панели, джойстику, кнопкам и viewport камеры
 * (GameScene), чтобы управление гарантированно не уходило под зону
 * home-индикатора даже там, где измеренный инсет чуть занижен.
 * Увеличь, если низ всё ещё прячется «под телефон».
 */
export const BOTTOM_EXTRA_LIFT = 50;
